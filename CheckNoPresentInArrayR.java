package Recursion;

/*public class CheckNoPresentInArrayR {
    static boolean count=false;
    public static void main(String[] args) {
        int[] a={1,2,3,4,5,6};
        int ele=44;

        boolean flag=false;
        flag=checkPresent(0,a,ele);
        if (!flag)
            System.out.println("found");
        else
            System.out.println("not found");
    }

    private static boolean checkPresent(int i, int[] a, int ele) {
        if(i<a.length)
        {
            if(a[i]==ele)
                 count true;
            if(count==false)
                checkPresent(i+1,a,ele);
            else
                return count;
        }
        return count;
    }
}*/
